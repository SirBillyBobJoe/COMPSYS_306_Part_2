[tool.black]
line-length = 130
target-version = ['py36']
include = '\.pyi?$'

[tool.pylint."MESSAGES CONTROL"]
disable = [
  "no-member",
  "import-error",
  "no-name-in-module",
  "useless-super-delegation",
  "too-few-public-methods",
]

[tool.pyright]
pythonVersion = '3.6.9'
venvPath = ''
venv = ''
